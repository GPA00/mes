# 全局换行符规范
* text=auto

# 批处理脚本必须强制保持 Windows CRLF 换行符（cmd.exe 指针偏移要求）
*.bat text eol=crlf
*.cmd text eol=crlf

# Shell 脚本与容器配置文件强制保持 Unix LF 换行符
*.sh text eol=lf
*.conf text eol=lf
*.cnf text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
Dockerfile text eol=lf
